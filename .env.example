# HTTP
PORT=3000

# PostgreSQL — all required. The server exits if it cannot connect at startup.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=employee_management
